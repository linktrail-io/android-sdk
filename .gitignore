# Build output / Gradle
**/build/
**/.gradle/
.gradle/

# Local config (Android SDK path, API key) — never commit
**/local.properties

# Signing keystores — never commit
*.jks
*.keystore

# IDE
.idea/
*.iml

# macOS
.DS_Store
